unit: protocol-dispatch
bindings:
  - framework: "IMessageHandler"
    source: "src/nro/models/server/Controller.java:25-49"
    must_appear_in_target: "Controller remains reachable from the session message path"
  - framework: "numeric game protocol"
    source: "src/nro/models/server/dispatch/ControllerCommandRegistry.java:13"
    must_appear_in_target: "Command IDs and payload ordering remain stable"
runtime_config:
  - source: "MySession.version"
    must_appear_in_target: "Version-dependent packet widths remain enforced"
